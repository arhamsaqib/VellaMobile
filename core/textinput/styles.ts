import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  input: {
    fontSize: 16,
    fontWeight: 'bold',
  },
  container: {
    marginBottom: 5,
    borderWidth: 1,
    borderRadius: 4,
    borderColor: 'grey',
  },
});
